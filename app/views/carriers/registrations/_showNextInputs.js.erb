<% content_for(:after_js) do %>
  <%= javascript_tag do %>
    document.addEventListener("DOMContentLoaded", function() {

      const btn =  document.querySelector('#showNextInputs');
      const backBtns = document.querySelectorAll("[data-action='previous']");
      btn.addEventListener('click', handleClick);
      backBtns.forEach(function(btn) { btn.addEventListener('click', handleClick) });

      function handleClick() {
        const firstInput =  document.querySelector("section[data-name='inputBlock1']");
        const secondInput =  document.querySelector("section[data-name='inputBlock2']");
        const isNotInView =  firstInput.classList.contains('notInView');
        const isInView =  secondInput.classList.contains('inView');
        isInView ? firstInput.classList.remove('notInView') : firstInput.classList.add('notInView');
        isInView ? secondInput.classList.remove('inView') : secondInput.classList.add('inView');
        $('html, body').animate({
          scrollTop: $('html, body').offset().top
        }, 400);
      }

    });
  <% end %>
<% end %>